#import <Foundation/Foundation.h>

/// Static, developer-reviewed search data generated from DYStorage scan files.
/// Each plug-in dictionary contains `plugin`, `aliases`, and `features`; each
/// feature contains `section` and `title`.
FOUNDATION_EXPORT NSArray<NSDictionary *> *DYStorageSearchCatalog(void);

